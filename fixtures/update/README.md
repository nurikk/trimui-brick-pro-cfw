# Update fixtures

These generated fixtures are synthetic and contain no device image, ROM, BIOS, or block-device data. `positive/manifest.json` and `payload.squashfs` form a valid exact-SKU/revision TG4040 userspace update with the six approved shared user-data classes and a three-copy peak-space declaration. The same files exercise online and sideload paths. Rejection manifests cover closed-schema, device/SKU, payload name/type/hash, and compatibility gates; the `update-agent journey` additionally covers truncated payloads, insufficient space, unsafe power, every journaled interruption boundary, resume, first boot, data preservation, and rollback.
