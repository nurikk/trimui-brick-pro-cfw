# Launch contract

The launch contract is a typed data handoff. Requests contain no executable command, shell fragment, arbitrary path, or process option. Catalog membership, package ownership, and protected-data boundaries are validated by their owning components before a session starts.
