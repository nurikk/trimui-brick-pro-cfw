Generated synthetic compatibility-recipe fixtures. The repository contains only public TUF metadata and project-authored metadata with dummy hashes. Private signing material was generated outside the repository and is not retained. No ROM, BIOS, corpus, package, executable, URL, path, or device data is present. The fixture binary verifies this offline chain and uses a disposable host root for protected-data and transaction checks.
