# Simulator smoke validation

The simulator smoke journey exercises launcher/session lifecycle, generated libretro and PortMaster demos, package install boundaries, protected data, suspend/resume, and recovery. It uses only generated fixture content and reports aggregate pass/fail results using only generated fixture data.
