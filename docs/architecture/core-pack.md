# Core pack boundary

The core-pack catalog is a blocked planning record. It carries no package payload or installable artifact. The package manager rejects core packages before extraction or activation, while the catalog keeps the explicit device, system, BIOS, routing, and protected-data checks needed by the launcher contract.
