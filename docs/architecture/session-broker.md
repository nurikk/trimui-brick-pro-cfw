# Session broker

The broker runs typed launcher requests through the selected adapter. PortMaster uses the package manager's private activation record and fixed entrypoint; no caller command, shell text, URL, global linker state, or system mutation reaches the child.

ROMs and user saves/states remain outside package lifecycle ownership. Settings and Save Vault data are protected boundaries and are never package destinations or removal targets.
